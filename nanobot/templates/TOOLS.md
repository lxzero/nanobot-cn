# 工具使用说明

工具签名通过函数调用自动提供。本文件记录非显而易见的限制和使用模式。

## exec — 安全限制

- 命令有可配置的超时时间（默认 60 秒）
- 危险命令被屏蔽（rm -rf、format、dd、shutdown 等）
- 输出截断至 10,000 个字符
- `restrictToWorkspace` 配置可将文件访问限制在工作区内

## cron — 定时提醒

- 请参阅 cron 技能以了解使用方法。
